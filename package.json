{
  "name": "gifclip",
  "version": "0.1.0",
  "private": true,
  "description": "Localhost lossless GIF slice editor",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "start:lan": "node server.js --host 0.0.0.0",
    "test": "node --test",
    "verify": "node scripts/verify-sample.js",
    "benchmark:preview": "node scripts/benchmark-preview.js"
  },
  "engines": {
    "node": ">=20"
  }
}
